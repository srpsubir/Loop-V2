Soft on/off toggle — terracotta when on, paper well when off.

```jsx
<Switch checked={on} onChange={setOn} label="Remind me about this crew" />
```

Props: `checked`, `onChange(next)`, `disabled`, optional `label`.
