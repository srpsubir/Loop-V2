Inset text field for Loop — sunk into the paper, no hard outline, warm focus ring.

```jsx
<Input label="Their name" placeholder="e.g. Priya Raman" />
<Input icon={<SearchIcon/>} placeholder="Search everyone…" />
```

Props: `label`, `icon` (leading), `placeholder`, `value`/`defaultValue`, `onChange`, `disabled`. Focus shows an inset shadow plus a soft terracotta ring.
