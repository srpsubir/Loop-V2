Soft tinted label for chapters, places and quiet status — never a status dot.

```jsx
<Tag tone="chapter">London years</Tag>
<Tag tone="positive" icon={<HeartIcon/>}>Still close</Tag>
```

Tones: `neutral`, `chapter` (terracotta), `people` (rose), `positive` (sage).
