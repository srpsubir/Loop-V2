Round portrait with warm initials fallback. Tint is derived from the name.

```jsx
<Avatar src={photo} name="Priya Raman" size={48} />
<Avatar name="Marcus Bell" ring="sage" />
```

Props: `src`, `name` (drives initials + tint), `size`, `ring` ("none" | "sage" | "terracotta").
