The fundamental container — borderless warm paper lifted by a soft shadow.

```jsx
<PaperCard padding={24}>…</PaperCard>
<PaperCard interactive onClick={open}>…</PaperCard>
```

Props: `raised` (surface tint), `interactive` (hover lift), `padding`, `onClick`. Never add a border — elevation comes from shadow alone.
